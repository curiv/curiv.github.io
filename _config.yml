theme: jekyll-theme-minimal
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one
title: curiv's homepage
description: Bookmark this to keep an eye on my project updates!
